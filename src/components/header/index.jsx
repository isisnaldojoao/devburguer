// Creating the import styles for the module header
import styles from './header.module.css'

// Function for call component in the project
function Header() {

    return (
    <header>
        <h1>DevBurger</h1>
    </header>
    );
  }
  
// Exporting component for the project
  export default Header;
  